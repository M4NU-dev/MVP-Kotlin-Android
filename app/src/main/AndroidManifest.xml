<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mvpkotlinandroid">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MVPKotlinAndroid">
        <activity
            android:name=".UI.DetailsArticle.DetailArticleActivity"
            android:label="@string/title_activity_detail_article"
            android:theme="@style/Theme.MVPKotlinAndroid.NoActionBar"></activity>
        <activity
            android:name=".UI.Main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.MVPKotlinAndroid.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>